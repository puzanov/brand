%h3 Новости

.admin
  %a{:href=>"/articles/new"}
    Редактировать новости

.news 
  - @articles.each do |article|
    .title 
      = article.title
    .brief
      = article.brief
    .image
      = image_tag ("/uploads/" + article.image)
