- @cat_name.each() do |row|
  %h3
    = row['name']
.prod_list
  %ul
    - @products.each() do |row|
      %li
        - if !row["ID"].nil?
          = link_to row["NAME"], '/product/products/' + row["ID"].to_s
  
